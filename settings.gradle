// Replace the overriding repositories url with proper url
pluginManagement {
    repositories {
        maven {
            url 'https://artifactory.com/artifactory/maven-release'
        }
    }
}

rootProject.name="Store Service"
